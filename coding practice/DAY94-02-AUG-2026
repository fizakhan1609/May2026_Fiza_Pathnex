# Day 01 - Basics

##    Ansible Task - Install Nginx on pathnex server

Rewrite this YAML manually:

```yaml
- name: Install Nginx on pathnex server 
  hosts: all
  becone: yes

  tasks:
    - name: Install nginx
      yum:
        name: nginx
        state: present


   Docker 
# Docker Installation & First Conatiner 
docker --version
docker run ubuntu echo "Hello Pathnex"


# Docker File
FROM ubuntu:22.04
CMD ["echo", "Hello Pathnex"]
