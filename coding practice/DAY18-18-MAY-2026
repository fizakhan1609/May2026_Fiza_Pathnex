Timings 

What I did today
3 hours - Kubernetes Upgrate live class
3 hour - Revised Same day class youtube
2  hr wth chatgtp search with examples 
1 hr min coding practice and explainanton 
30 min interview practice and viedo making
1 hr  min evening class 
 

Tomorrow's Task
3 hr HELM ARGOCR
2 hr revised 
1 hr notes theory and create own notes for underanding  
1 hr coding practice and explainanton
1 hr interview practice and viedo making
1 hr in evenig class

# Day 18 - High Availability. Multi Region, and Auto-scaling

##    Ansible - Configure HAProxy for Load Balancing

```yaml
- name: Setup HAProxy Load Balancer
   hosts: all
  beccome:yes
  tasks:
    - name: Install HAProxy
      yum:
        name: haproxy
        state: present
    - nam: Configure HAProxy
      template:
        src: haproxy.cfg.j2
        dest: /etc/haproxxy/haproxy.cfg
    - name: Start HAProx service
      service:
        name: started
        state: started
        enabled: yes
## Teraform -- Multi-region Auto-scaling Group with ALB
resource "aws_lb" "pathnex_lb" {
  name               = "pathnex-lb"
  internal           = false
  load_balancer_type = "application"
  security_group     = [aws_secuity_group.allow_ssh_http.id]
  subnets            = [aws_subnet.public.id, aws_subnet.public2.id]
}

resource "aws_autosclling_group" "pathnex_asg" {
  desired_capacity     = 3 
  max_size             = 5
  min_size             = 
  vpc_zone_identifier  = [aws_subnet.public.id. aws_subnet.public2.id]
  launch_configuration = aws_launch_configuration.pathnex_conig.id
}
## Jubernetes - Horizontal pod Autoscaler with Multuple Replices
apiersion: apps/vs1
kind: Deployment
metadata:
  name: athnex-web-app
space
  replicas 3 
  selector:
    matchLabels:
      app: pathnex-web-app
  template:
   metadata:
     lables:
        app" athnex-web-app
    spec:
     containers:
       - nameL nginx
         image: ngnx
         ports:
            - containerPort: 80
---

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: pathnex-web-app-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pathnex-web-app
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
## Jenkinsfile — Blue-Green Deployment Strategy
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building Docker image...'
                docker.build('pathnex-web-app')
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
            }
        }
        stage('Blue-Green Deployment') {
            steps {
                script {
                    sh 'kubectl apply -f kubernetes/blue-deployment.yaml'
                    sh 'kubectl apply -f kubernetes/green-deployment.yaml'
                    sh 'kubectl rollout status deployment/blue-deployment'
                    sh 'kubectl rollout status deployment/green-deployment'
                }
            }
        }
    }
}
## GitLab CI/CD — Blue-Green Deployment with Kubernetes
stages:
  - build
  - push
  - deploy

build:
  stage: build
  script:
    - docker build -t pathnex-web-app .

push:
  stage: push
  script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"
    - docker push pathnex-web-app

deploy:
  stage: deploy
  script:
    - kubectl apply -f kubernetes/blue-deployment.yaml
    - kubectl apply -f kubernetes/green-deployment.yaml
    - kubectl rollout status deployment/blue-deployment
    - kubectl rollout status deployment/green-deployment



## Docker
# Redis Container
docker run -d --name pathnex-redis redis

# Real Path
/data
