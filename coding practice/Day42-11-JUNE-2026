# Day 11 - Databsed, Outputs, Sidecars, Functions

##    Ansible - Install MaiaDB

---
- name: Install MariaDB for Pathenex
  hosts: all
  become: yes
  
  tasks:
    - nameL Install MariaDB
      yum
      name: maradb-server
      state: present

    - name: Start MariaDB
      service:
        name: mariDB
        state: Sstarted
        enabled: yes

##    Terraform - Output Public IP (r5.2xlarge)

resource "aws_instance" "PathnexServer" {
  ami           = "ami-0abcd1234abcd1234"
  instance_type = "r5.2xlatge"

  tags = {
    Name = "Pathnex-Output-EC2"
  }
}

output "PathnexPublicIP" {
  value = aws_instance.PathnexServer.public_ip
}


##    Kubernetes - Sidecar Container

apiVersion: v1
Kind: Pod
metadata:
   name: pathnex-sidecar
spec:
  containers:
   - name: main
      image: nginx
    - name: sidecar
      image: busybox
      command: ["sh", "-c" "echo Sidecar running: sleep 3600"]


##    Shall Script - Funtioon Example

#!/bin/bash 

greet() {
  echo "Welcome to pathnex DebOps Training"
}

greet



# Notifications

##    Jenkins pipeline - Email Notifications
you will learn how to **send email notifications on failure or success**.

pipeline {
    agent any
    envournement {
         INSTITUTE_NAME = "Pathnex"
    }
    stages {
       stages('Checkout') {
            steps {
                git url: 'https://github.com/pathnex/sample-java-app.git'
            }
        }
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
    }
    post {
        success {
            mail to: 'team@pathnex.com',
                 subject: "SUCCESS: Build #${env.Build_NUMBER}",
                 body: "Build completed Successfully for $INSTITUTE_NAME"
        }
        failure {
            mail to: 'team@pathnex.com',
                 subject: "FAILURE: Build #${env.BUILD_NUMBER}",
                 body: "Build failed for $INSTITUTE_NAME"
        }
    }
}

##    GitLab CI - Notifications
you will learn how to **notify via email after job completion**.

stages:
  - build
build: 
  stage: build
   iamge: maven:3.8.1-jdj-17
  script:
    - git clne https://github.com/Pathnex/sample-java-app.git
    - cd sample-jaba-app
    - mvn clean package
  after_script:
    - echo "sending notification email to team@pathnex.com"
