

# Day 17 - Multi-cloud Deployments, Auto-healing, and Advanced CI/CD

## Ansible -- Auti healing web servers with systemd 
```yaml
 - name: confgure Auto-healig fr web server
   hosts: all
   become: yes 
   tasks:
    - name: Ensure nginx service is running
        name: nginx
        state: started
        enabble: yes
    - name: Setup auto- restart for ngix service
      lineinfile:
        enabled: yes
    - name: setup auto restart for nginx service
            lininfile:
        path: /ect/systemd/system/nginx.service.d/override.conf
        line: "Restart==always"
        create: yes
    - name: Reload systemd
      comand:  sysemctl deaemon-reload
## Tereaform - Multi-cloud Infrastructure with ASW and Azure
 resource "aws_vpc" "main" {
  cidr_block = "10.0.0.0/16"
}

# Azure VNet
resource "azuerm_virtual_network" "main" {
  name                = "pathnex-vnet"
  location            = "East US"
  address_space       = ["10.0.0.0/16"]
  resource_group_name = azurerm_resource_group.main.name 

# EC2 instance in AWS
resource "aws_instance" "pathnex_ec2" {
  ami           = "ami-0abcd1234abcd1234"
  instance_type = "t2.micro"
  region        = "us-east-1"
}

# Virtual Machine in Azure
resource "azurerm_linux_virtual_machine" "pathnex_vm" {
  name               = "pathnez-azure-vm"
  resource_group_name = azurerm_resource_group.main-name
  location            =  azreem_resource_group.main.locatiom
  size                = azurerm_resuorce_group.main.location
  admin_username      = "azureuser"
  admisn_assword      + p@assword1231"
}
## Kubernetes -- Self-Healng with Liveness and Readiness probes
aiVesion: aps/v1
kind: Deplyment
metadata:
  name: pathnex-web-app
space:
  replicas: 3
  selector:
    mathLabels:
      app: pathnex-web-app
  template:
    metadata:
      lables:
        app: pathnex-web-app
    space:
       cntainers:
        - name: nginx
          image: nginx
          posrts:
            - containerports: 80
          livenessProbe:
            httpGet:
              path: /healthz
              port:80
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port:80
            initialDelaySeconds: 5
            periodSeconds: 10
## Jenksfile - Rolling Deployments with Kubernetes
pipelne {
    agent any 
    aniverment {
        IMAGE_NAME = 'pathnex-web-app'
  } 
   stage {
        stage('build') {
            steps {
               echo 'Build Docker iamage...'
                docker.build('${env.IMAGE_NAME}")
            }
        }
        stage('Test') {
           steps {
                echo 'Running tests...'
            }
        }
        stage('Deploy to Kubernetes') {
            steps {
               script {
                 scripts {
                    sh 'kubectl apply -f kubernetes/deployment.yaml'
                    sh 'kuberctl rollout status deployment/pathnex-web-app'
                }
            }
        }
    }
}
## GitLab CI/CD - Multi-Cloud Deployment
stages:
  - build 
  - push
  - deploy

buid:
  stage: build
  dcript:
   - docker build - t pathnex-web-app -

push:
  stage: push
  scipt:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"

- dockder push pathnex-web-app

deploy_aws:
  stage: deploy
  scipts:
    - kubectl --context=aws-cluster apply -f kubernetes/deployment.yaml

deploy_azure:
  stage: deploy
  script:
    - kubectl --context=azure-cluster apply -f kubernetes/deployment.yaml
 
